package me.superischroma.spotifywrapper;

/**
 * Determines an enumeration entry provides an underlying value accessible through getValue().
 */
public interface ValueIndicator
{
    String getValue();
}